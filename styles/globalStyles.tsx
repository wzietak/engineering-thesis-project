import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    deckContainer: {
        flex: 1,
        width: '80%',
        height: '20%',
        alignItems: 'center',
		justifyContent: 'center',
		padding: 3,
    }
});